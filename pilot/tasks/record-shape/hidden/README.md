# 숨은 표본 — 세션에게 주지 않는다

채점기가 `data/` 자리에 이것을 갈아 끼우고 도구를 실행한다. 보이는 표본만으로
채점하면 기대 출력을 코드에 박아 넣은 세션과 실제로 고친 세션이 안 갈린다.

`expected/` 는 레퍼런스 해답으로 만든 기대 출력이다. 만드는 방법은
`solutions/README.md` 에 적혀 있다. 손으로 고치지 않는다.
