meterhouse is our usage metering tool. `README.md` has the layout and the
document list.

Something is wrong with the September run. The number of readings that come
back from `intake` doesn't match the number of data rows in the feed files —
some rows are neither counted nor reported as skipped, so they vanish without
a trace. Billing noticed because a site's total came in low.

Can you find out what's happening and fix it? Don't change what the tool is
supposed to do; the specs under `docs/` still stand.

When you wrap up, update `HANDOFF.md` — what you did, what you decided, what's
left, and what the next person should look at first. Only add to the
"decisions" section; don't remove what's already there.
