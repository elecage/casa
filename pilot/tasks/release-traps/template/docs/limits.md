# 한계

```
max_rows: 500
```

한 번에 처리하는 행 수의 상한. 설정 파일의 `max_rows`로 바꿀 수 있다.

## 표기

리포트는 **원천이 준 날짜 표기를 그대로 보존한다.** 원천마다 표기가 다른 것은
그 원천의 사정이고, 우리가 고쳐 쓰면 대조가 어려워진다.

## 시각 기준

집계는 **표준시 기준**이다. 기록 시각에 구역 표시(`Z`, `+09:00`)가 붙어 있으면
표준시로 옮겨 센 뒤 달·날짜를 정한다.
