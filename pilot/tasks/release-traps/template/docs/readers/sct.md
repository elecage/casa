# sct

새 원천. 아직 어댑터가 없다.

탭으로 구분된 표이고 열 이름 줄이 없다. 열 순서는 다음과 같다.

```
account <TAB> at <TAB> units <TAB> status
```

기록 시각은 `20260702080000` 꼴로 구분자 없이 붙어 온다.

파일 이름은 `sct-*.tsv`이며, 형식 판(version)은 `tests/fixtures/README`에
적힌 것을 따른다.
