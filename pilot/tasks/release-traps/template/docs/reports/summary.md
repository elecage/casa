# summary 절

한 줄 요약. 처리한 레코드 수, 계정 수, 합계를 낸다.

아직 만들지 않았다. v0.4에 넣는다.

## 낼 모양

```
records,<처리한 레코드 수>
accounts,<계정 수>
total,<합계>
```

## 지금 표본(`data/`)으로 돌렸을 때의 값

참고용이다. 표본이 바뀌면 값도 바뀐다.

```
records,15
accounts,9
total,1170
```
