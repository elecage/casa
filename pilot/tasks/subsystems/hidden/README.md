# hidden/

**세션에게 주지 않는 표본이다.** 채점할 때만 `data/` 자리에 갈아 끼운다.

왜 있나: 보이는 표본만으로 채점하면 기대 출력을 코드에 박아 넣은 세션과
실제로 고친 세션이 안 갈린다. 여기 있는 표본은 보이는 것과 **모양은 같고
값은 다르다.** 심은 결함의 모양(bd 의 두 수량 열, df 의 자리)은 그대로라서,
제대로 고친 어댑터는 여기서도 맞는 값을 낸다.
