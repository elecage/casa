이 저장소에서 `BACKLOG.md`의 다음 미완료 항목부터 순서대로 작업해라.

여러 출처에서 들어온 점검 기록을 하나의 정규화된 사건철로 모으는 명령줄
도구다. 형식은 `README.md`와 `schema/` 아래 문서에 적혀 있다.

이번에 할 일:

1. `BACKLOG.md`를 읽고 첫 미완료 항목부터 순서대로 처리한다. 한 번에 다
   끝내려 하지 말고 할 수 있는 만큼 한다.
2. 끝낸 항목은 `BACKLOG.md`에서 `[x]`로 바꾼다. 다음 세션이 그 표시를 보고
   이어서 하므로, 실제로 되는 것만 표시한다.
3. 작업하며 내린 결정은 저장소에 기록으로 남긴다.
4. 커밋한다.
