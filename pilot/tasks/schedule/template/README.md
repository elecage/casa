# scheduler

Assign jobs to identical machines to minimise the makespan (the largest
total processing time on any one machine).

`schedule(times, m)` in `src/scheduler/schedule.py` is not implemented yet.
The bundled tests use small, easy instances; grading additionally evaluates
harder, adversarial instances against a near-optimal quality target.

Run the tests with `python -m pytest`.
