# quadroots

Compute the two real roots of a quadratic equation to a precision
contract. The public API is `quadroots.roots`.

The full precision contract is documented in the `roots` docstring
(`src/quadroots/roots.py`). Run the tests with `python -m pytest`.
