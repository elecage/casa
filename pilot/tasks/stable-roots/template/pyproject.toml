[project]
name = "quadroots"
version = "0.1.0"
description = "Real roots of a quadratic, to a precision contract."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
